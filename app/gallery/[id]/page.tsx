import Image from"next/image";import Link from"next/link";import{notFound}from"next/navigation";import SiteHeader from"@/components/site-header";import{formatGalleryDate,getPublicGalleryAlbum}from"@/lib/gallery";export default async function GalleryAlbumPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const data=await getPublicGalleryAlbum(id);if(!data)notFound();return <main><SiteHeader/><section className="gallery-album-heading"><Link href="/gallery">← Back to Gallery</Link><p className="eyebrow">School Gallery</p><h1>{data.album.title}</h1><p>{formatGalleryDate(data.album.event_date)}</p>{data.album.description&&<p>{data.album.description}</p>}</section><section className="gallery-image-grid">{data.images.map(image=>image.signed_url&&<figure key={image.id}><Image src={image.signed_url} alt={image.alt_text} width={900} height={700} sizes="(max-width: 700px) 90vw, 45vw"/>{image.caption&&<figcaption>{image.caption}</figcaption>}</figure>)}</section></main>}
