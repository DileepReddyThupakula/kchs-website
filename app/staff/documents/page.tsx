import Link from"next/link";import{documentCategories,documentCategoryLabels,formatFileSize,getStaffDocuments}from"@/lib/documents";export default async function Documents({searchParams}:{searchParams:Promise<{q?:string}>}){const p=await searchParams;const{items,failed}=await getStaffDocuments(p.q);return <><header className="staff-page-header"><div><p className="eyebrow">School records</p><h1>Documents</h1><p>Manage school downloads and public access.</p></div><Link className="staff-create-link" href="/staff/documents/new">+ Upload document</Link></header><form className="staff-filters"><label className="staff-search-label">Search documents<input name="q" defaultValue={p.q??""}/></label><button>Search</button></form><section className="staff-table-wrap">{failed?<div className="staff-empty"><h2>Documents are unavailable</h2></div>:items.length?<table className="staff-table"><thead><tr><th>Document</th><th>Category</th><th>Status</th><th>File</th><th/></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><strong>{x.title}</strong></td><td>{documentCategoryLabels[x.category]}</td><td>{x.status}</td><td>{x.original_filename} · {formatFileSize(x.file_size)}</td><td><Link className="staff-open-link" href={`/staff/documents/${x.id}/edit`}>Edit →</Link></td></tr>)}</tbody></table>:<div className="staff-empty"><h2>No documents yet</h2><Link href="/staff/documents/new">Upload document</Link></div>}</section></>}
