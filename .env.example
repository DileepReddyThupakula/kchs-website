# Required for Phase 3 persistent admissions storage. This must be server-only
# and point to a serverless-compatible Postgres provider; never commit a value.
DATABASE_URL=

# Optional notification after a successfully persisted enquiry. It must never be
# the source of truth for admissions storage.
ADMISSIONS_WEBHOOK_URL=
