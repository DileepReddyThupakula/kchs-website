# Optional Phase 2 integration: an HTTPS endpoint that securely stores and/or
# notifies the school about a validated admission enquiry.
# Do not expose this value to the client or commit a real endpoint.
ADMISSIONS_WEBHOOK_URL=
